### Encryption Key Management

{{companyShortName}} uses AWS Key Management Service (KMS) for encryption key management.

- KMS keys are unique to {{companyShortName}} environments and services.

- KMS keys are automatically rotated yearly.